# Fission Backend — Environment
# Copy to backend/.env and fill in.

# Server
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=development
CORS_ORIGINS=http://localhost:5173

# Canton Ledger API (LocalNet defaults)
LEDGER_API_URL=http://localhost:2975

# Keycloak (LocalNet defaults — override for DevNet)
KEYCLOAK_TOKEN_URL=http://keycloak.localhost:8082/realms/AppProvider/protocol/openid-connect/token
KEYCLOAK_REALM_URL=http://keycloak.localhost:8082/realms/AppUser
KEYCLOAK_CLIENT_ID=app-provider-validator
KEYCLOAK_CLIENT_SECRET=AL8648b9SfdTFImq7FV56Vd0KHifHBuC

# Parties — populate from the bootstrap script output
OPERATOR_PARTY=
CUSTODIAN_PARTY=
ORACLE_PARTY=
