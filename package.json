{
  "name": "fission-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Yield tokenization for tokenized RWAs on Canton Network",
  "license": "Apache-2.0",
  "workspaces": [
    "backend",
    "frontend",
    "services/oracle",
    "services/sequencer",
    "scripts"
  ],
  "scripts": {
    "build": "make daml-build && cd backend && npm run build && cd ../frontend && npm run build",
    "test": "make daml-test",
    "lint": "cd backend && npm run lint"
  }
}
